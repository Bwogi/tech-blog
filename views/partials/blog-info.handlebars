    {{!-- <div class="mt-4">
        <h4><strong><a href="/blogs/{{id}}">{{title}}</a></strong></h4>
        <p>Posted by {{user.username}} on {{format_date created_at}}</p>
            <p class="text-muted blog-text">
              {{blog_text}}
            </p>          
    </div> --}}

  <div class="card mb-4 my-4">
  <div class="card-header text-light" style="background-color:#f8f8f8"><h4><strong><a href="/blogs/{{id}}">{{title}}</a></strong></h4></div>
  <div class="card-body">
    <blockquote class="blockquote mb-0">
      <p class="text-muted blog-text">
        {{blog_text}}
      </p>
      <footer class="blockquote-footer">
        Posted by <cite title="Source Title"><strong>{{user.username}}</strong> on {{format_date created_at}}</cite>
      </footer>
    </blockquote>
  </div>
</div>
        